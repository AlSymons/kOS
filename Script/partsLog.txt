 
PART(KR-2042 (Mun SpamSat),uid=3801022464)
    kOSProcessor
        (get-only) kos disk space, is Int32
        (get-only) kos average power, is Single
        (callable) open terminal, is KSPEvent
        (callable) toggle power, is KSPEvent
        (callable) open terminal, is KSPAction
        (callable) close terminal, is KSPAction
        (callable) toggle terminal, is KSPAction
        (callable) toggle power, is KSPAction
    ModuleLight
        (get-only) light status, is String
        (callable) lights off, is KSPEvent
        (callable) toggle light, is KSPAction
        (callable) turn light on, is KSPAction
        (callable) turn light off, is KSPAction
    kOSLightModule
        (get-only) required power for lights, is Single
        (settable) light r, is Single
        (settable) light g, is Single
        (settable) light b, is Single
        (get-only) power starved, is Boolean
        (get-only) last requested power, is Single
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(probeCoreOcto,uid=3587096576)
    ModuleCommand
        (get-only) comm signal, is String
        (get-only) comm first hop dist, is String
        (get-only) command state, is String
        (settable) hibernation, is Boolean
        (settable) hibernate in warp, is Boolean
        (callable) control from here, is KSPEvent
        (callable) rename vessel, is KSPEvent
        (callable) control from here, is KSPAction
        (callable) toggle hibernation, is KSPAction
    ModuleReactionWheel
        (settable) reaction wheels, is Int32
        (settable) wheel authority, is Single
        (get-only) reaction wheels, is String
        (callable) toggle torque, is KSPEvent
        (callable) cycle wheel mode, is KSPAction
        (callable) activate wheel, is KSPAction
        (callable) deactivate wheel, is KSPAction
        (callable) toggle rw, is KSPAction
    ModuleSAS
    ModuleKerbNetAccess
        (callable) kerbnet access, is KSPEvent
        (callable) kerbnet access, is KSPAction
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
    ModuleSPU
        (get-only) spu, is String
    ModuleRTAntennaPassive
    ModuleTripLogger
 
PART(miniFuelTank,uid=4150427648)
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(miniFuelTank,uid=1635270656)
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(batteryPack,uid=2198601728)
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(batteryPack,uid=1156595712)
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(liquidEngineMini,uid=1719926784)
    ModuleEnginesFX
        (get-only) fuel flow, is Single
        (get-only) prop. requirement met, is Single
        (get-only) thrust, is Single
        (get-only) specific impulse, is Single
        (get-only) status, is String
        (settable) thrust limiter, is Single
        (callable) shutdown engine, is KSPEvent
        (callable) toggle engine, is KSPAction
        (callable) shutdown engine, is KSPAction
        (callable) activate engine, is KSPAction
    ModuleJettison
        (callable) jettison shroud, is KSPAction
    ModuleGimbal
        (settable) gimbal, is Boolean
        (settable) gimbal limit, is Single
        (callable) show actuation toggles, is KSPEvent
        (callable) toggle gimbal, is KSPAction
        (callable) lock gimbal, is KSPAction
        (callable) free gimbal, is KSPAction
        (callable) toggle gimbal pitch, is KSPAction
        (callable) toggle gimbal yaw, is KSPAction
        (callable) toggle gimbal roll, is KSPAction
    ModuleTestSubject
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(batteryPack,uid=1410646016)
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(batteryPack,uid=1973977088)
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(SurfAntenna,uid=3351232512)
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
    ModuleRTAntenna
        (get-only) energy, is String
        (get-only) omni range, is String
        (get-only) status, is String
        (settable) target, is String | Body | Vessel
        (callable) deactivate, is KSPEvent
        (callable) toggle, is KSPAction
        (callable) activate, is KSPAction
        (callable) deactivate, is KSPAction
    ModuleSPUPassive
    ModuleRTDataTransmitter
        (get-only) comms, is String
 
PART(solarPanels4,uid=3914563584)
    ModuleDeployableSolarPanel
        (get-only) sun exposure, is Single
        (get-only) energy flow, is Single
        (get-only) status, is String
        (callable) toggle solar panel, is KSPAction
        (callable) extend solar panel, is KSPAction
        (callable) retract solar panel, is KSPAction
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(solarPanels4,uid=3837706240)
    ModuleDeployableSolarPanel
        (get-only) sun exposure, is Single
        (get-only) energy flow, is Single
        (get-only) status, is String
        (callable) toggle solar panel, is KSPAction
        (callable) extend solar panel, is KSPAction
        (callable) retract solar panel, is KSPAction
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(solarPanels4,uid=106070016)
    ModuleDeployableSolarPanel
        (get-only) sun exposure, is Single
        (get-only) energy flow, is Single
        (get-only) status, is String
        (callable) toggle solar panel, is KSPAction
        (callable) extend solar panel, is KSPAction
        (callable) retract solar panel, is KSPAction
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(solarPanels4,uid=1743405056)
    ModuleDeployableSolarPanel
        (get-only) sun exposure, is Single
        (get-only) energy flow, is Single
        (get-only) status, is String
        (callable) toggle solar panel, is KSPAction
        (callable) extend solar panel, is KSPAction
        (callable) retract solar panel, is KSPAction
    TweakScale
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(sensorThermometer,uid=2306736128)
    ModuleEnviroSensor
        (get-only) display, is String
        (callable) toggle display, is KSPEvent
        (callable) toggle display, is KSPAction
    ModuleScienceExperiment
        (callable) log temperature, is KSPEvent
        (callable) log temperature, is KSPAction
        (callable) delete data, is KSPAction
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(sensorGravimeter,uid=150290432)
    ModuleEnviroSensor
        (get-only) display, is String
        (callable) toggle display, is KSPEvent
        (callable) toggle display, is KSPAction
    ModuleScienceExperiment
        (callable) log gravity data, is KSPEvent
        (callable) log gravity data, is KSPAction
        (callable) delete data, is KSPAction
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
 
PART(sensorBarometer,uid=713621504)
    ModuleEnviroSensor
        (get-only) display, is String
        (callable) toggle display, is KSPEvent
        (callable) toggle display, is KSPAction
    ModuleScienceExperiment
        (callable) log pressure data, is KSPEvent
        (callable) log pressure data, is KSPAction
        (callable) delete data, is KSPAction
    GeometryPartModule
    FARAeroPartModule
    FARPartModule
    KOSNameTag
        (get-only) name tag, is String
        (callable) change name tag, is KSPEvent
